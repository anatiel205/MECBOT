{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python3 bot_placas_prod.py",
    "healthCheckPath": "/",
    "healthCheckTimeout": 60
  }
}
